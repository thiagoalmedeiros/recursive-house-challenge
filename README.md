# Rhc

This project was developed for the Recursive House selection process.

## Execution
To run the project, perform the following command at the root of the project: `ng serve` or `yarn start`.

Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.
